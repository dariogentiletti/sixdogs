{
  "name": "sixdogs",
  "version": "0.1.0",
  "private": true,
  "description": "SIXDOGS: WARDOGS community Discord bot, its core service and the website.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "postinstall": "npm --prefix core install --omit=dev --no-audit --no-fund && npm --prefix bot install --omit=dev --no-audit --no-fund",
    "start": "node run.mjs",
    "build": "node tools/build.mjs",
    "test": "npm --prefix core test && npm --prefix bot test"
  }
}
