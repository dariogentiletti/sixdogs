{
  "name": "sixdogs",
  "version": "0.1.0",
  "private": true,
  "description": "SIXDOGS: WARDOGS community Discord bot, its core service and the website.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "postinstall": "node tools/postinstall.mjs",
    "start": "node run.mjs",
    "build": "node tools/build.mjs",
    "test": "node --test cloudflare/test/*.test.js && npm --prefix core test && npm --prefix bot test"
  }
}
