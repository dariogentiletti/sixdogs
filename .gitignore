# Your settings and passwords. Never commit these: the repo is public.
.env

# Installed per machine, not part of the project.
node_modules/

# The built-in database (only used when DATABASE_URL is unset).
data/
data-practice/

# Marker the bot writes after uploading its avatar.
bot/assets/.avatar-applied

*.log
__pycache__/
*.pyc
.DS_Store
